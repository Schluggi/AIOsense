substitutions:
  buzzer_pin: GPIO9  # untested
  led_pin: GPIO15
  pir_pin: GPIO11
  sen0395_io_pin: GPIO3
  uart_rx_pin: GPIO37
  uart_tx_pin: GPIO39

esp32:
  board: esp32-s2-saola-1
  variant: ESP32S2
  framework:
    type: arduino

i2c:
  sda: GPIO33
  scl: GPIO35

sensor:
  - platform: internal_temperature
    name: "CPU Temperature"
